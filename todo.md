# Rosmontic 界面逐屏复刻清单

- [ ] 锁定启动面板、输入框、底部操作栏与图标的尺寸、颜色、圆角和阴影。
- [ ] 复刻开始页的白色留白、蓝青 320px 面板与中文输入提示。
- [ ] 复刻战斗页的 200px 左侧单位栏、细生命条、属性行、分隔线和文字日志。
- [ ] 复刻快进、返回、分享等灰色控制按钮的视觉位置与状态。
- [ ] 对照参考页面截图验证桌面与移动端，并修正可见差异。
- [ ] 将完成的前端构建结果同步至 GitHub Pages 项目。
- [ ] 从参考 `index.html` 与战斗文档中映射启动页、面板、单位栏、日志和控制区的 DOM 结构。
- [ ] 独立重建与参考布局相近的 React 组件和 CSS，而不引入原项目源码或资源。
- [ ] 实现保留输入顺序的队伍解析、特殊名字识别和确定性伪随机种子。
- [ ] 实现首版属性生成、回合事件流、生命结算和结果表渲染。
- [ ] 在参考站点进行多局实际对战，记录并校准战斗态 UI。
- [ ] 将名字自身哈希与完整对局顺序哈希拆分为两个稳定、可移植的种子。
- [ ] 保证同一名字在任何输入位置的八项基础属性完全一致。
- [ ] 保证改变完整输入顺序会影响行动、目标与事件流，但不改变任何名字的基础属性。
- [ ] 为双种子契约加入可重复执行的回归测试与样例输出。
- [ ] 以 C++ 定义身份哈希、对局种子、属性、状态、事件与结算的唯一权威接口。
- [ ] 使用 Emscripten 将 C++ 战斗核心编译为 WebAssembly。
- [ ] 让 TypeScript/React 仅提交输入和渲染 C++ 返回的事件，禁止在前端重算战斗结果。
- [ ] 为 C++ 内核建立相同输入稳定复现、顺序变化影响对局的回归测试。
- [x] 在 C++ 中实现原始 UTF-8 文本回显、字节长度、空行分队、顺序列表与传输哈希输出。
- [x] 为中文、英文、特殊字符、重复名字、空行分队和 CRLF 输入建立 C++ 解析测试。
- [x] 在接入浏览器前以本地可执行测试确认 C++ 通信模块输出稳定。
- [x] 所有 C++ 文件使用 `bits/stdc++.h`、4 格缩进、`#define int long long` 与 `&&`、`||` 运算符。
- [x] 仅在 Windows 目标条件下包含 `windows.h`，避免影响 WebAssembly 构建。
- [x] 所有固定长度整数数组改用 `array<int, N>`，业务数组和编号统一采用 1-index。
- [x] 整数变量遵循 `int a = 0 , b = 0 , c = 0;` 的同类合并声明格式。
- [x] 固定数组统一写作 `array<int , N>`；指针与排序范围统一经 `.data()` 取得。
- [x] 一号下标排序统一使用 `sort(arr.data() + 1 , arr.data() + n + 1)` 形式。
- [x] 条件判断与花括号统一采用 `if(a % 2 == 0)` 后左花括号独立换行的格式。
- [ ] 函数定义、参数列表、引用与逗号空格统一按用户指定的竞赛码风整理。
- [ ] 完整 C++ 头文件、实现、测试和 CMake 构建文件作为可编译源码交付。
- [x] 定义所有角色与 Boss 共用的 `Player` 结构体，涵盖名称、阵营、完整属性、生命、技能、状态、资源与战斗标记。
- [x] 定义 `Skill`、`StatusEffect`、`Team` 与 `BattleSnapshot` 结构体。
- [x] 建立由技能直接写入前端的 `RenderCommand` 指令、分行与颜色提示接口。
- [x] 编译验证结构体均使用 1-index 的业务编号和用户指定码风。
- [x] 使用 C++ SHA-256 为每个 UTF-8 名字生成完整 256 位身份摘要。
- [x] 从回显解析结果生成名字、队伍、输入位置与身份摘要的只读前端响应。
- [x] 验证同名摘要稳定、单字符变化发生雪崩变化、顺序变化不改变单个名字摘要。
- [x] 保持名字身份摘要与未来对局随机种子、属性生成和战斗事件流职责分离。
- [x] 从身份摘要派生当前生命、最大生命、物理攻击、防御、魔法攻击、防御、智慧、速度与魔力。
- [x] 所属队伍只来自前端输入的分队解析，不从名字身份摘要派生。
- [x] 将派生值填入统一 Player，并保持技能槽、当前技能槽、眷属和状态列表为空。
- [x] 验证同名属性稳定、不同位置不变、队伍随输入分队变化。
- [x] 在 C++ 将 Player 基础属性序列化为前端只读 JSON 快照。
- [x] 导出 UTF-8 输入到属性快照的 WebAssembly 调用接口。
- [x] 编译并测试 C++/WASM 接口返回与原生 C++ 快照一致。
- [x] 前端只提交文本并原样渲染 C++ 返回的属性快照。
- [x] 将基础属性范围固定为生命 200–400、物攻 70–130、物防 30–60、速度 800–1200、魔攻 0–80、魔防 0–100、智慧 100–200、魔力 0–100。
- [x] 使用名字身份摘要派生的确定性截断正态分布替换均匀范围映射。
- [x] 验证同名稳定、全量边界、均值居中与极端值稀少等分布特征。
- [x] 重新编译 WASM，并确认前端显示新范围内的 C++ 属性快照。
- [ ] 整理并打包当前 C++ 头文件、实现、测试、CMake 与 WebAssembly 构建脚本。
- [ ] 附上源码模块用途与本地编译说明，便于用户自行维护。
- [ ] 保留 C++ 运行核心：公共头、Player/Skill 结构、输入解析、身份哈希、属性派生、快照、WASM 导出和 WASM 构建脚本。
- [ ] 删除本地构建目录、原生/Node 测试、CMake 配置、旧说明与重复码风文档。
- [ ] 重新执行 WASM 构建与网页调用，验证精简后仍可运行。
- [x] 为所有公共头文件补充模块、结构体字段、枚举、1-index 和接口中文注释。
- [x] 为输入解析、SHA-256、正态属性派生和 JSON 快照实现补充逐段中文说明。
- [x] 为 WASM 导出函数与构建脚本补充前端调用、内存生命周期和参数解释。
- [x] 重新构建 WASM，确认注释后代码行为不变。
- [x] 删除 `BattleEvent`、事件类型枚举、事件数据槽和战斗快照中的事件时间线字段。
- [x] 定义不含时间字段的 `RenderCommand`，仅保存技能直接发送的文本、颜色、换行、目标和数值。
- [x] 提供技能内可直接调用的渲染指令输出函数与顺序化命令列表。
- [x] 重新编译 WASM，确认现有属性快照接口不受影响。
- [x] 新增 C++ 基础结算模块，定义 `heal`、`damage`、`add_status` 与 `settle_status` 的统一接口。
- [x] 实现治疗上限、伤害护盾吸收、死亡判定及直接渲染指令输出。
- [x] 实现状态合并/叠层、层数上限、持续回合与状态文本输出。
- [x] 实现持续伤害、持续治疗、到期移除与状态属性修正的结算。
- [x] 编译 WASM 并执行基础结算函数回归测试。
- [x] 将输入页改为荧光框风格，只保留名字输入和单一“开始”按钮。
- [x] 将战斗页重组为左侧单位血条栏与右侧战斗播放区。
- [x] 保留 C++ 属性快照调用，不在前端改变任何战斗数值。
- [x] 验证桌面与移动端的荧光框布局、按钮和属性快照显示。
- [x] 移除输入页网格、断角、刻度、伪终端标签与复杂按钮装饰，仅保留发光细框。
- [x] 将战斗页同步简化为左血条栏和右播放区的低装饰发光线框。
- [x] 验证简化版发光框在桌面与移动端的可读性和布局。
- [x] 将开始页背景改回白色，并将主框改为圆角发光框。
- [x] 将开始按钮与战斗页控制性元素统一为圆角，保持名字输入框直角。
- [x] 将左血条栏与右播放区同步为白底圆角框线视觉。
- [x] 验证白底圆角框在桌面与移动端的可读性。
- [x] 将速度属性派生范围调整为 800–1200，并更新相关属性快照验证。
- [x] 实现普通攻击技能：从当前技能槽首项选择，输出蓝色“攻击”描述后调用 `damage`。
- [x] 实现角色行动变量、统一加速与大于 20000 后逐个执行并减 20000 的规则。
- [x] 保证每次统一加速后，先清空所有当前就绪角色的操作，再开始下一轮加速。
- [x] 为行动顺序、阈值边界、普通攻击和伤害渲染指令编写回归测试。
- [x] 实现 C++ 高速完整战斗推进：一次调用连续处理所有瞬间，直到胜负已定。
- [x] 将玩家最终生命与 `RenderCommandBuffer` 序列化为 C++ 战斗模拟 JSON。
- [x] 导出 `name_arena_simulate_battle` WebAssembly 接口并加入构建白名单。
- [x] 在前端开始操作中调用战斗模拟接口，并将 C++ 指令显示在右侧播放区。
- [x] 验证页面仅播放 C++ 完整结算返回的全部攻击、伤害、状态和结算指令，不展示内部加速瞬间。
- [x] 移除前端可见的“造成伤害”文本，仅保留其 C++ 内部结算结果。
- [x] 将普通攻击描述和承受者受到伤害描述合并为同一行，并在整行末尾换行。
- [x] 将“受到”及“点伤害”保持普通颜色，仅将伤害数字输出为红色指令片段。
- [x] 更新 C++、WASM 与前端回放测试，验证新的攻击显示顺序和颜色语义。
- [x] 将前端播放从逐条指令间隔改为按完整技能行与完整受伤行推进。
- [x] 在扣血事件中先显示红色待失去血量段，再以加速动画将其抹除为最终血量。
- [x] 使用 C++ 指令的 `valueAfter` 驱动生命变化，不在前端重算伤害或治疗数值。
- [x] 验证连续攻击时事件顺序、红色损失段和移动端血条显示。
- [x] 移除右侧战斗播放日志，只保留左侧血条与右侧基础属性列表。
- [x] 人数不超过 10 时列出每名角色的八项基础属性。
- [x] 人数超过 10 时切换为仅显示名字的紧凑列表。
- [x] 验证两种人数模式的桌面与移动端布局。
- [x] 恢复左血条、右战斗播放的主布局。
- [x] 将不超过十人的八项属性压缩为战斗界面上方的简略信息行。
- [x] 将超过十人的属性栏保持为上方仅名字的紧凑列表。
- [x] 验证播放动画、紧凑属性栏与移动端双栏折叠显示。
- [x] 收紧上方属性信息行和右侧战斗日志的行距。
- [x] 延长红色待失去血量段的停留时间与加速抹除动画时长。
- [x] 验证连续受伤时生命降低过程的清晰度与文本密度。
- [x] 从完整原始输入顺序派生 C++ 对局种子，并初始化确定性随机数状态。
- [x] 将普通攻击目标改为在全部存活敌人中确定性随机选取。
- [x] 保证相同输入顺序目标序列相同，改变输入顺序可改变目标序列。
- [x] 为随机目标、可复现与顺序变化编写 C++ 和 WebAssembly 回归测试。
- [ ] 确认用户指定的 GitHub 目标仓库与当前远程状态。
- [ ] 核查将要提交的 name-arena 源码、WASM 产物和构建配置，排除本地缓存与测试二进制。
- [ ] 创建 Git 提交并推送到目标 GitHub 仓库。
- [ ] 验证远程提交包含当前完整战斗、随机目标和界面版本。
- [ ] 将当前项目内容同步到已确认的 `alpharchmage/alpharchmage.github.io` 目标仓库根目录。
